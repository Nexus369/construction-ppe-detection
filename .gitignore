# Python
__pycache__/
*.py[cod]
*.so
.Python
env/
venv/
ENV/
*.egg-info/

# Environment secrets
.env
# Backups of it are just as sensitive as the original, and get made under
# time pressure — before editing CORS origins for a new tunnel, say. The
# bare `.env` pattern does not cover `.env.bak.1787086950`, so one careless
# `git add -A` would publish every key in it.
.env.*
!.env.example
backend/app.db
*.db
# One pattern for every ESP32 sketch folder, current and future — each
# copies secrets.h.example to secrets.h locally, and none of those should
# ever be trackable regardless of which sketch folder they live in.
**/secrets.h
# And backups of it, which carry the same WiFi password and device
# credentials. `**/secrets.h` does not match `secrets.h.bak` — the same
# gap that left a plaintext .env backup trackable earlier.
**/secrets.h.*
!**/secrets.h.example

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# Local dev artifacts
output/
runs/
.vercel
# Flask's request log — every URL hit, including anything passed as a query
# param; noise at best, a credential leak at worst.
backend/app.log
backend/server.log
backend/server.err.log
# Added by code-review-graph
.code-review-graph/

# AI coding-tool local config (installed per-machine, not project state)
.claude/
.codebuddy/
.cursor/
.gemini/
.qoder/
.mcp.json
opencode.jsonc

# Stored refusal frames — personal data, not source
backend/instance/

# Written by the desktop launcher at runtime. The log is whatever the
# gate printed on this particular Pi, and the lock file only exists
# while it is running.
pi_app/checkpoint.log
pi_app/.checkpoint.lock
